---
description: "Task list for Phase 1 — Init Base with Module Placeholders"
---

# Tasks: Phase 1 — Init Base with Module Placeholders

**Input**: Design documents from `specs/001-init-base-module-placeholder/`
**Prerequisites**: plan.md ✅, spec.md ✅, research.md ✅, data-model.md ✅, contracts/ ✅, quickstart.md ✅

**Tests**: Not included — no tests requested in the feature specification for Phase 1.

**Organization**: Tasks grouped by user story to enable independent implementation and testing.

## Format: `[ID] [P?] [Story?] Description`

- **[P]**: Can run in parallel (different files, no dependencies on incomplete tasks)
- **[Story]**: Which user story this task belongs to (US1 = Student Shell, US2 = FSD Isolation)
- Exact file paths in every description

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Scaffold the Vite + React + TypeScript + Tailwind + Biome project from scratch.
All subsequent tasks depend on this phase being complete first.

- [X] T001 Scaffold Vite React TypeScript project at repository root: run `pnpm create vite@latest . --template react-ts`, choose "Ignore files and continue" to preserve existing `.specify/`, `.github/`, `specs/` directories, then run `pnpm install`
- [X] T002 Install Tailwind CSS 4 Vite plugin: run `pnpm add -D tailwindcss @tailwindcss/vite`, then update `vite.config.ts` to add `tailwindcss()` to the plugins array alongside the existing `react()` plugin
- [X] T003 Replace the contents of `src/index.css` with a single line: `@import "tailwindcss";` and remove all other generated Tailwind directives or base CSS
- [X] T004 Add `@types/node` dev dependency and configure path alias: run `pnpm add -D @types/node`, then add `resolve: { alias: { "@": path.resolve(__dirname, "./src") } }` to `vite.config.ts`, and add `"baseUrl": ".", "paths": { "@/*": ["./src/*"] }` to `compilerOptions` in both `tsconfig.json` and `tsconfig.app.json`
- [X] T005 Initialise shadcn/ui: run `pnpm dlx shadcn@latest init`, select style **Default**, base color **Slate**, and CSS variables **Yes**; verify `components.json` and `src/lib/utils.ts` are created
- [X] T006 Move shadcn utility to FSD shared layer: move `src/lib/utils.ts` to `src/shared/lib/utils.ts`, update the `utils` alias in `components.json` to `@/shared/lib/utils`, and delete the now-empty `src/lib/` directory
- [X] T007 Install and configure Biome: run `pnpm add --save-dev --save-exact @biomejs/biome && pnpm dlx @biomejs/biome init`, then replace `biome.json` content with the config from `specs/001-init-base-module-placeholder/quickstart.md` Step 5, and add `lint`, `format`, `check`, `type-check` scripts to `package.json`
- [X] T008 Remove ESLint artifacts generated by the Vite template: delete `eslint.config.js` (or `.eslintrc*`) and `eslint.config.ts` if they exist; verify `pnpm check` exits with 0 errors on the initial scaffold
- [X] T009 Install React Router DOM: run `pnpm add react-router-dom`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Create the `entities/module` layer and `shared/config/routes.ts`. These are
consumed by both user story 1 (sidebar + routing) and user story 2 (FSD structure
validation). No feature slice or widget can be implemented without these.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete.

- [X] T010 Create directory structure for all FSD layers: create `src/app/`, `src/pages/module-view/ui/`, `src/widgets/sidebar/ui/`, `src/features/basic-c-syntax/ui/`, `src/features/data-types/ui/`, `src/features/input-output/ui/`, `src/features/functions/ui/`, `src/features/if-statement/ui/`, `src/features/for-loop/ui/`, `src/features/arrays/ui/`, `src/features/strings/ui/`, `src/features/file-handling/ui/`, `src/features/examples/ui/`, `src/entities/module/model/`, `src/entities/module/config/`, `src/shared/ui/`, `src/shared/config/`
- [X] T011 Create `src/entities/module/model/module.types.ts` with `ModuleStatus` type (`"available" | "coming-soon"`), `Module` interface (`id`, `slug`, `title`, `order`, `status`), and `ModuleRegistry` type (`readonly Module[]`) — exactly as specified in `data-model.md`
- [X] T012 Create `src/entities/module/config/modules.ts` with the `MODULE_REGISTRY` constant containing all 10 modules in order: basic-c-syntax (order 1) through examples (order 10), all with `status: "available"` — exact values from `data-model.md`
- [X] T013 Create `src/entities/module/index.ts` barrel export: `export type { Module, ModuleStatus, ModuleRegistry } from "./model/module.types"` and `export { MODULE_REGISTRY } from "./config/modules"`
- [X] T014 Create `src/shared/config/routes.ts` with the `ROUTES` constant: `ROOT: "/"`, `MODULE: (slug: string) => \`/modules/\${slug}\``, `MODULE_PATTERN: "/modules/:moduleSlug"` — as specified in `contracts/routing.md`

**Checkpoint**: Foundation ready — entities and routing constants exist. User story phases can now begin.

---

## Phase 3: User Story 1 — Student Sees the Learning Platform Shell (Priority: P1) 🎯 MVP

**Goal**: A fully navigable dashboard shell with all 10 module entries in the sidebar,
each linking to a placeholder content panel on the right. Active module is highlighted.
App deep-links work and default route redirects correctly.

**Independent Test**: Run `pnpm dev`, open the browser, verify: sidebar shows 10 modules
in correct order, each click updates the right panel with the correct module title, the
active item is highlighted, `pnpm check` exits 0.

### Implementation for User Story 1

- [X] T015 [P] [US1] Create `src/features/basic-c-syntax/ui/BasicCSyntaxPage.tsx` — zero-prop React component that renders module title "Basic C Syntax" and a "Coming soon" placeholder message using shadcn/ui Card or simple div with Tailwind classes
- [X] T016 [P] [US1] Create `src/features/data-types/ui/DataTypesPage.tsx` — same pattern as T015, title "Data Types"
- [X] T017 [P] [US1] Create `src/features/input-output/ui/InputOutputPage.tsx` — same pattern as T015, title "Input & Output"
- [X] T018 [P] [US1] Create `src/features/functions/ui/FunctionsPage.tsx` — same pattern as T015, title "Functions"
- [X] T019 [P] [US1] Create `src/features/if-statement/ui/IfStatementPage.tsx` — same pattern as T015, title "If Statements"
- [X] T020 [P] [US1] Create `src/features/for-loop/ui/ForLoopPage.tsx` — same pattern as T015, title "For Loop Statements"
- [X] T021 [P] [US1] Create `src/features/arrays/ui/ArraysPage.tsx` — same pattern as T015, title "Arrays"
- [X] T022 [P] [US1] Create `src/features/strings/ui/StringsPage.tsx` — same pattern as T015, title "Strings"
- [X] T023 [P] [US1] Create `src/features/file-handling/ui/FileHandlingPage.tsx` — same pattern as T015, title "File Handling"
- [X] T024 [P] [US1] Create `src/features/examples/ui/ExamplesPage.tsx` — same pattern as T015, title "Examples"
- [X] T025 [P] [US1] Create all 10 feature barrel `index.ts` files: each exports only its page component — e.g. `src/features/basic-c-syntax/index.ts` exports `BasicCSyntaxPage`, repeat for all 10 slices (can be done as one task since all 10 follow the identical pattern)
- [X] T026 [US1] Create `src/widgets/sidebar/ui/SidebarItem.tsx` — component accepting `module: Module`, `isActive: boolean`, `onClick: () => void` props; renders a `<NavLink>` or accessible button styled with Tailwind; active state uses a distinct background/text color; keyboard-navigable (proper `role`, `aria-current="page"` when active)
- [X] T027 [US1] Create `src/widgets/sidebar/ui/Sidebar.tsx` — component accepting `SidebarProps` (`modules: readonly Module[]`, `activeSlug: string`); renders a scrollable `<nav>` list of `SidebarItem` components; sidebar scroll MUST NOT affect the content panel
- [X] T028 [US1] Create `src/widgets/sidebar/index.ts` barrel: `export { Sidebar } from "./ui/Sidebar"` and `export type { SidebarProps } from "./ui/Sidebar"`
- [X] T029 [US1] Create `src/pages/module-view/ui/ModuleViewPage.tsx` — reads `moduleSlug` from `useParams()`, looks up active module in `MODULE_REGISTRY`, renders two-panel layout (`<Sidebar>` on left + matched feature page component on right); renders a 404 "Not found" panel when slug is unknown; layout is responsive (Tailwind flex/grid, min-width clamp or breakpoint for ≥375 px)
- [X] T030 [US1] Create `src/pages/module-view/index.ts` barrel: `export { ModuleViewPage } from "./ui/ModuleViewPage"`
- [X] T031 [US1] Create `src/app/router.tsx` — define `AppRouter` with `<BrowserRouter>`, route `/` redirecting to `/modules/basic-c-syntax`, route `/modules/:moduleSlug` rendering `<ModuleViewPage />`, and catch-all `*` also rendering `<ModuleViewPage />` for 404 handling — as specified in `contracts/routing.md`
- [X] T032 [US1] Create `src/app/App.tsx` — root component that renders `<AppRouter />`; import and mount global CSS (`src/index.css`)
- [X] T033 [US1] Update `src/main.tsx` — replace the default Vite scaffold content to mount `<App />` using `createRoot`; ensure `src/index.css` is imported

**Checkpoint**: At this point User Story 1 is fully functional. Run `pnpm dev`, open
`http://localhost:5173`, verify all 10 sidebar items work and the content panel updates.

---

## Phase 4: User Story 2 — Developer Can Extend Any Module Independently (Priority: P2)

**Goal**: Every C concept module lives in its own isolated FSD feature slice with a clear
public API (`index.ts` barrel). The structure is verifiable by inspection and Biome passes
with zero errors confirming no cross-slice or cross-layer violations.

**Independent Test**: Edit one module's placeholder text (e.g. add "Hello World" to
`BasicCSyntaxPage.tsx`), reload app — only that module's panel changes. Run `pnpm check`
and `pnpm type-check` — both exit 0.

### Implementation for User Story 2

- [X] T034 [US2] Verify FSD import layer rules are respected across all created files: review all `import` statements in `src/` to confirm no feature imports from another feature, no entity imports from a widget or feature, and no shared layer imports from any other layer; fix any violations found during T015–T033
- [X] T035 [US2] Add `vite.config.ts` import boundary comment block documenting the FSD layer rules (app → pages → widgets → features → entities → shared) for developer reference, matching the rules in `contracts/module-api.md`
- [X] T036 [US2] Run final `pnpm check` (Biome lint + format) and `pnpm type-check` (TypeScript strict); fix any remaining zero-tolerance errors so both commands exit with code 0; commit-ready state

**Checkpoint**: All user stories complete. FSD structure is intact, Biome clean, TypeScript
strict passes. Project is ready for Phase 2 module content development.

---

## Phase 5: Polish & Cross-Cutting Concerns

**Purpose**: Final quality pass — accessibility, responsive layout, clean dev experience.

- [X] T037 Add `aria-label="C Learning Modules"` to the sidebar `<nav>` element and `aria-current="page"` to the active sidebar item; verify Tab key cycles through all 10 items and Enter/Space activates navigation (WCAG 2.1 AA — FR-010, Constitution VI)
- [X] T038 Add a `<title>` update on route change so the browser tab shows the active module name (e.g. "Basic C Syntax — LearnC"); use `useEffect` + `document.title` or a lightweight `<Helmet>`-equivalent; keep it dependency-free if possible
- [X] T039 Verify responsive layout at 375 px, 768 px, 1280 px viewports in browser DevTools; adjust Tailwind classes in `ModuleViewPage.tsx` and `Sidebar.tsx` if any breakpoint produces overflow or illegible text; document the final Tailwind breakpoint strategy in a brief comment in `ModuleViewPage.tsx`
- [X] T040 Clean up all Vite template boilerplate files that are no longer needed: remove `src/assets/react.svg`, `public/vite.svg`, `src/App.css` (if it exists and was not already replaced), and the default `App.tsx` counter example content — leaving only the files created or modified in this phase

---

## Dependencies (User Story Completion Order)

```
Phase 1 (Setup: T001–T009)
    ↓
Phase 2 (Foundational: T010–T014)
    ↓
Phase 3 (US1: T015–T033) ← T015–T025 can run in parallel within this phase
    ↓
Phase 4 (US2: T034–T036) ← depends on US1 structure being in place
    ↓
Phase 5 (Polish: T037–T040)
```

**Parallelizable within Phase 3** (T015–T025 all touch separate files):
- T015–T024 (10 placeholder page components) — all independent of each other
- T025 (barrel exports) — can be done alongside T015–T024 or after

**Cannot parallelize**:
- T026–T033 are sequential (Sidebar depends on Module types, Page depends on Sidebar, Router depends on Page, App depends on Router)
- T034–T036 must follow T033 (need the full structure to verify)

---

## Parallel Execution Examples

**Example: Two-developer split**

| Developer A | Developer B |
|---|---|
| T001 → T002 → T003 → T004 → T005 → T006 → T007 → T008 → T009 | — (wait for Phase 1) |
| T010 → T011 → T012 → T013 → T014 | — (wait for Phase 2) |
| T015–T019 (5 feature pages) | T020–T024 (5 feature pages) |
| T025 (barrel exports) | — |
| T026 → T027 → T028 → T029 → T030 | — |
| T031 → T032 → T033 | T034 → T035 → T036 |
| T037 → T038 → T039 → T040 | — |

---

## Implementation Strategy

**MVP Scope** (User Story 1 only — Phases 1–3):
Tasks T001–T033 deliver the fully functional navigable shell. This is the complete
Phase 1 deliverable and independently demonstrable.

**Full Phase 1 completion** (Phases 1–5):
Adds FSD isolation verification (US2) and accessibility/polish pass. Recommended before
merging to main.

**Task count summary**:
- Phase 1 (Setup): 9 tasks (T001–T009)
- Phase 2 (Foundational): 5 tasks (T010–T014)
- Phase 3 (US1): 19 tasks (T015–T033)
- Phase 4 (US2): 3 tasks (T034–T036)
- Phase 5 (Polish): 4 tasks (T037–T040)
- **Total: 40 tasks**

**Parallel opportunities**: 11 tasks in Phase 3 can run concurrently (T015–T025).
